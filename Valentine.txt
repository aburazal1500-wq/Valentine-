<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Angelina 💖 Valentine</title>

<style>
body {
    margin: 0;
    font-family: 'Poppins', sans-serif;
    background: linear-gradient(135deg, #ff4d6d, #ff8fab);
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}

.card {
    background: white;
    padding: 30px 25px;
    border-radius: 20px;
    width: 90%;
    max-width: 360px;
    text-align: center;
    box-shadow: 0 25px 50px rgba(0,0,0,0.25);
    animation: pop 0.6s ease;
}

@keyframes pop {
    from { transform: scale(0.8); opacity: 0; }
    to { transform: scale(1); opacity: 1; }
}

h1 {
    color: #ff2f7b;
    font-size: 24px;
}

p {
    color: #555;
    font-size: 16px;
    margin: 15px 0 25px;
}

.buttons {
    position: relative;
    height: 60px;
}

button {
    padding: 12px 26px;
    font-size: 16px;
    border: none;
    border-radius: 30px;
    cursor: pointer;
}

#yes {
    background: #ff2f7b;
    color: white;
}

#yes:hover {
    transform: scale(1.1);
}

#no {
    position: absolute;
    left: 60%;
    background: #ddd;
}

.confetti {
    position: fixed;
    top: -10px;
    font-size: 24px;
    animation: fall 3s linear infinite;
}

@keyframes fall {
    to {
        transform: translateY(110vh) rotate(360deg);
        opacity: 0;
    }
}
</style>
</head>

<body>

<div class="card">
    <h1>Angelina 💖</h1>
    <p>
        Will you be my Valentine? 🌹<br>
        Be honest… you don’t really have a choice 😌💘
    </p>

    <div class="buttons">
        <button id="yes" onclick="yesClicked()">Yes 💘</button>
        <button id="no" onmouseover="moveNo()" ontouchstart="moveNo()">No 🙈</button>
    </div>
</div>

<script>
function moveNo() {
    const noBtn = document.getElementById("no");
    const x = Math.random() * 260 - 130;
    const y = Math.random() * 160 - 80;
    noBtn.style.transform = `translate(${x}px, ${y}px)`;
}

function yesClicked() {
    for (let i = 0; i < 40; i++) {
        createConfetti();
    }

    document.body.innerHTML = `
        <div style="
            background: linear-gradient(135deg, #ff4d6d, #ff8fab);
            height:100vh;
            display:flex;
            align-items:center;
            justify-content:center;
            text-align:center;
            color:white;
            font-family:Poppins;
        ">
            <div>
                <h1 style="font-size:42px;">SHE SAID YES!!! 🎉😂</h1>
                <p style="font-size:22px;">Me right now:</p>
                <p style="font-size:60px;">🕺💃🥳🔥</p>
                <p style="font-size:20px;">Best decision ever, Angelina 💖</p>
            </div>
        </div>
    `;
}

function createConfetti() {
    const confetti = document.createElement("div");
    confetti.classList.add("confetti");
    confetti.style.left = Math.random() * 100 + "vw";
    confetti.innerHTML = ["🎉","💖","😂","💘","🔥"][Math.floor(Math.random()*5)];
    document.body.appendChild(confetti);

    setTimeout(() => {
        confetti.remove();
    }, 3000);
}
</script>

</body>
</html>